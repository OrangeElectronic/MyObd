<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blelibrary">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Demo.Myapp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MainAppTheme">

        <activity
            android:name=".Demo.SelectAction"
            android:theme="@style/SwipTheme" />
        <activity
            android:name=".Demo.ScanBle"
            android:theme="@style/SwipTheme" />
        <activity android:name=".MainActivity.MainPeace">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity.ScanBle"
            android:theme="@style/SwipTheme"
            />
        <service
            android:name=".Demo.blelibrary.Server.BluetoothLeService"
            android:enabled="true" />
        <service
            android:name=".blelibrary.Server.BluetoothLeService"
            android:enabled="true" />
    </application>

</manifest>